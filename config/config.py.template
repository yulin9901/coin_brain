# Configuration for Crypto Trading Strategy System
# Copy this file to config.py and fill in your details.

# OpenAI API Key for AI decisions (via Sealos AI Proxy)
OPENAI_API_KEY = "YOUR_OPENAI_API_KEY_HERE"
SEALOS_API_URL = "https://api.sealos.run/openai/v1/chat/completions"  # Sealos AI Proxy URL

# Binance API Configuration
BINANCE_API_KEY = "YOUR_BINANCE_API_KEY_HERE"
BINANCE_API_SECRET = "YOUR_BINANCE_API_SECRET_HERE"
BINANCE_TESTNET = True  # Set to False for real trading

# MySQL Database Configuration
DB_HOST = "localhost"
DB_PORT = 3306
DB_USER = "your_db_user"
DB_PASSWORD = "your_db_password"
DB_NAME = "coin_brain"  # Changed from stock_analysis

# News API Sources
CRYPTOPANIC_API_KEY = "YOUR_CRYPTOPANIC_API_KEY_HERE"  # CryptoPanic API密钥
COINMARKETCAL_API_KEY = "YOUR_COINMARKETCAL_API_KEY_HERE"  # CoinMarketCal API密钥
COINMARKETCAL_X_API_KEY = "YOUR_COINMARKETCAL_X_API_KEY_HERE"  # CoinMarketCal X-API-KEY

# 定时任务配置
# 加密货币市场24/7运行，无需考虑交易日
# 此设置保留用于可能的维护窗口或特定时间段
RUN_ALWAYS = True

# 数据收集频率配置
# 每小时数据收集时间（分钟）
HOURLY_COLLECTION_MINUTE = 0

# 每日汇总和策略生成时间（小时:分钟）
# 在每天的这个时间点运行数据汇总和获取交易建议
DAILY_STRATEGY_TIME = "00:05"  # UTC时间，建议选择交易量较低的时段

# 交易执行配置
# 是否启用自动交易执行
ENABLE_AUTO_TRADING = False  # 设置为True启用自动交易（谨慎使用）

# 风险管理配置
DEFAULT_RISK_PERCENTAGE = 2.0  # 默认风险百分比（每笔交易的风险占总资金的百分比）
MAX_OPEN_POSITIONS = 5  # 最大同时开放仓位数量
MIN_TRADE_AMOUNT = 10.0  # 最小交易金额（USDT）
MAX_LEVERAGE = 3.0  # 最大杠杆倍数

# 价格监控配置
ENABLE_PRICE_MONITORING = True  # 是否启用价格监控
PRICE_UPDATE_INTERVAL = 1  # 价格更新间隔（秒）
WEBSOCKET_RECONNECT_INTERVAL = 30  # WebSocket重连间隔（秒）

# 止盈止损配置
DEFAULT_STOP_LOSS_PERCENTAGE = 2.0  # 默认止损百分比
DEFAULT_TAKE_PROFIT_PERCENTAGE = 4.0  # 默认止盈百分比
ENABLE_TRAILING_STOP = False  # 是否启用移动止损

# 交易对配置
# 主要交易对列表 - 系统将监控和交易这些币种
TRADING_PAIRS = [
    "BTCUSDT",    # 比特币
    "ETHUSDT",    # 以太坊
    "SOLUSDT",    # Solana
    "LINKUSDT",   # Chainlink
	"DOGEUSDT",   # Dogecoin
	"DOTUSDT",    # Polkadot
	"FETUSDT",    # Fetch.ai
	"TAOUSDT",    # Theta Network
	"INJUSDT",    # Injective Protocol
    "BNBUSDT",    # Binance Coin
    "ADAUSDT",    # Cardano
    "MATICUSDT",  # Polygon
    "AVAXUSDT",   # Avalanche
    "UNIUSDT"     # Uniswap
]

# 高优先级交易对 - 这些币种会优先处理和监控
HIGH_PRIORITY_PAIRS = [
    "BTCUSDT",
    "ETHUSDT",
    "SOLUSDT",
    "LINKUSDT",
	"DOGEUSDT",
	"DOTUSDT",
	"FETUSDT",
	"TAOUSDT",
	"INJUSDT"
]

# 低优先级交易对 - 这些币种在资源充足时处理
LOW_PRIORITY_PAIRS = [
    "BNBUSDT",
    "ADAUSDT",
    "MATICUSDT",
    "AVAXUSDT",
    "UNIUSDT"
]

# 新闻收集相关的币种符号（不带USDT后缀）
CRYPTO_SYMBOLS = [
    "BTC", "ETH", "SOL", "LINK", "DOGE", "DOT", "FET", "TAO", "INJ",
    "BNB", "ADA", "MATIC", "AVAX", "UNI"
]

# 市场数据收集配置
# K线数据收集的时间间隔
KLINE_INTERVALS = ["1m", "5m", "1h", "1d"]

# 不同时间间隔的数据量限制
KLINE_LIMITS = {
    "1m": 100,    # 1分钟K线，获取100条
    "5m": 200,    # 5分钟K线，获取200条
    "1h": 500,    # 1小时K线，获取500条
    "1d": 1000    # 1天K线，获取1000条
}

# 回测配置
BACKTEST_ENABLED = True  # 是否启用回测功能
BACKTEST_DAYS = 30  # 回测天数
BACKTEST_PAIRS = ["BTCUSDT", "ETHUSDT", "SOLUSDT"]  # 回测使用的交易对

# Server酱微信推送配置
# 获取方式：
# 1. 访问 https://sct.ftqq.com/ 注册账号
# 2. 关注"Server酱"微信公众号
# 3. 在网站上获取SendKey
# 4. 将SendKey填入下面的配置中
SERVERCHAN_SENDKEY = "YOUR_SERVERCHAN_SENDKEY_HERE"  # Server酱的SendKey
ENABLE_WECHAT_REPORT = True  # 是否启用微信报告推送
DAILY_REPORT_TIME = "08:00"  # 每日报告发送时间（24小时制，北京时间）
WECHAT_REPORT_TITLE = "CoinBrain每日报告"  # 微信报告标题

# 报告内容配置 - 可以选择性开启/关闭各个模块
REPORT_INCLUDE_MARKET_SUMMARY = True  # 包含市场概况
REPORT_INCLUDE_TRADING_STRATEGY = True  # 包含交易策略
REPORT_INCLUDE_PROFIT_LOSS = True  # 包含盈亏统计
REPORT_INCLUDE_NEWS_SUMMARY = True  # 包含新闻摘要
REPORT_INCLUDE_TOP_PERFORMERS = True  # 包含表现最佳的币种

# 微信推送高级配置
WECHAT_RETRY_TIMES = 3  # 发送失败时的重试次数
WECHAT_RETRY_DELAY = 5  # 重试间隔（秒）
WECHAT_TIMEOUT = 30  # 请求超时时间（秒）

# 数据保留策略配置
# 数据清理任务执行时间
DATA_CLEANUP_TIME = "02:00"  # 每日数据清理时间（24小时制，建议选择低峰时段）

# 各类数据的保留天数
HOT_TOPICS_RETENTION_DAYS = 30  # 热点话题数据保留天数
MARKET_FLOWS_RETENTION_DAYS = 7  # 市场资金流向数据保留天数
KLINE_RETENTION_DAYS = 30  # K线数据保留天数

# 数据归档配置（可选）
ENABLE_DATA_ARCHIVING = False  # 是否启用数据归档功能
ARCHIVE_RETENTION_DAYS = 90  # 归档前数据保留天数

